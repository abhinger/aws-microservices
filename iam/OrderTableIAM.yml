OrderTableIAM:
  Effect: Allow
  Action:
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:GetItem
    - dynamodb:UpdateItem
    - dynamodb:Query
  Resource:
    - ${self:custom.OrderTable.arn}
    - !Join ['/', ['${self:custom.OrderTable.arn}', 'index', 'buyerEmail']]
